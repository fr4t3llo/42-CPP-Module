#include "RPN.hpp"

int main(int ac, char **av)
{
    std::vector<int> vc;
    if (ac == 2)
    {
        
    }
    else 
    {
        std::cout << "Error arg\n" << std::endl;
        exit (1);
    }
    return (0);
}